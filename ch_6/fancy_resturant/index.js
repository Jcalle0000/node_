// import {fastify} from 'fastify'; // used as a health check to verify whether the server started successfully



// const app = fastify(serverOptions);

// we add routes to the server
// app.get('/' , async function homeHandler(){
//     return{
//         api:'fastify-resturant-api',
//         version:1
//     };
// });


// port var in order to select where the server listens for http requests
// const port = process.env.PORT||3000;

// start the server by calling the listen method
// await app.listen({host:'0.0.0.0',port});
